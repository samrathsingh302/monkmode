---
status: done
agent: b2-closeout
goal: Close out B2 — ARCHITECTURE.md row → mitigated, extend the elevated smoke test with tamper-repair checks (author only, no live run)
outcome: ARCHITECTURE.md §4 B2 row MITIGATED w/ residuals + §3 note (c). run-smoketest.ps1 extended (+12 checks → expect 27/27; T1 delete-block w/ sentinel, T2 blank-hosts, snapshot lifecycle, convergence/no-churn) and stale dist path fixed (repo moved to Atlas\repos\); cleanup.ps1 + teardown now delete the snapshot. Both scripts parse clean (PS 5.1). No repo code touched; suite stays 81/81.
gotchas: Fresh-eyes verifier did NOT run (session token limit hit, 0 tokens back) — scripts are parse-checked + author-self-reviewed only; the elevated run is the real verification. Smoke test now needs a 3-min block (was 2) — tamper section worst-case ~110s must fit inside it; don't shorten --for 3. Teardown/cleanup kill the service BEFORE restoring hosts — keep that order, a live service would self-heal the block back in over the restore. T2 destroying the sentinel is BY DESIGN (snapshot only owns our block) — don't "fix" it into a failure.
carry-on: "Next slice options (one per session): (a) Samrath runs the extended smoke test elevated (tools\build-dist.ps1 then monkmode-smoketest\run-smoketest.ps1, expect 27/27, paste log; fix any failures); (b) Phase 3 B1 watchdog (service ⇄ helper restart pair). Phase 2 (THREATMODEL.md) remains DEFERRED until Samrath's explicit go."
---

# 12/06/2026 (night) — B2 close-out

## What happened
Continuation of the CV-readiness wave's carry-on: did options (c) + the
authoring half of (a) as one slice — single-writer, no fan-out (too small/
coupled to split; the planned verifier subagent was blocked by the session
token limit).

## Delivered
1. **ARCHITECTURE.md** — §4 B2 row rewritten as MITIGATED 12/06/2026 with the
   honest residuals (snapshot deletable; ~10s tick window; B2 chained to B1);
   §4 dated status note; §3 trust model gains "(c) restores hosts entries from
   the CLI snapshot every 10s".
2. **run-smoketest.ps1** (out-of-repo, `C:\Users\samra\monkmode-smoketest\`) —
   stale `C:\Users\samra\projects\...` dist path fixed; block 2→3 min; user
   sentinel planted pre-block; new §2b: T1 (clear attrib + delete our block →
   restored ≤35s, sentinel preserved, RO re-asserted, sinkhole live again,
   hosts byte-identical 12s later = no churn) and T2 (blank entire hosts →
   block restored); snapshot existence + verbatim-parity checks in §2;
   "snapshot deleted after lift" in §4; teardown removes leftover snapshot.
   15 → 27 expected checks.
3. **cleanup.ps1** — deletes the snapshot too (prevents a reinstalled service
   resurrecting a previous block's sites).

## Evidence
- Test assertions grounded in code, not the spec: `Service1.vb` timer repair
  gate (~232–254), `RepairHostsBlock` semantics (~340–357), `stopMe()` snapshot
  delete (~393), `Blocker.WriteHostsBlock` snapshot parity (~179–196).
- Both scripts: PowerShell 5.1 parser reports 0 errors (parse-only check; the
  fence forbids running them un-asked — they edit live hosts + install the
  service).
