---
status: done
agent: b1-layer2-guardian
goal: Phase 3 B1 layer 2 — wire the mutual service ⇄ guardian watchdog pair per the locked decision (A) SYSTEM-child guardian; tests with the code; fail-closed via BlockHasExpired; NO live runs (fence).
outcome: Shipped. New MM_guard project → mm_guard.exe (SYSTEM-session guardian, spawned by the service's timer through the tested ShouldRestartPeer gate); guardian SCM-restarts a killed MONKMODE and relaunches mm_notify into the user session (WTSQueryUserToken+CreateProcessAsUser); stands down only on a genuinely parsed, past Until; stopMe() kills it at expiry. Suite 95→123 green; fresh-context verifier SHIP (0 P0/P1; 2 actionable P3s fixed same-session, 1 documented as residual). Commits 395782c (impl) + 879e24b (P3 fixes) + docs. B1 is now mitigated-in-software, BOTH layers NOT live-verified.
gotchas: The expiry teardown order matters — guardian checks expiry FIRST each tick, and stopMe kills the guardian; the stopMe ⇄ SCM-recovery interaction (could a self-stop at expiry trigger a layer-1 restart loop?) was reasoned + verifier-walked but NEVER run live — the smoke test must assert "expiry → service stays stopped, no mm_guard left". A user-session mm_guard launch: non-elevated dies instantly at the Global mutex (by design, graceful); ELEVATED pre-pinning neutralises layer 2 (documented residual, B10-adjacent, layer 1 still covers). Guardian cadence (10s/5s) is pinned by tests to Service1.TimerIntervalMs/ExpiryGraceSeconds — retune both halves together. Per-project Simple3Des/IniFile copies now exist in 4 projects (4-copy ciphertext-equivalence test pins them); the guardian's DecryptData returns "" on bad Base64 (never End).
carry-on: "Next slice: EXTEND the elevated smoke test (C:\Users\samra\monkmode-smoketest\run-smoketest.ps1, currently 27 checks) for B1 both layers, then Samrath runs it elevated. New checks: sc qfailure MONKMODE shows the policy (3× restart/1s/INFINITE); taskkill /f the service → SCM restarts it ≤~5s and mm_guard reappears ≤10s; taskkill /f mm_guard → service respawns it ≤10s; taskkill /f mm_notify → guardian relaunches it into the user session ≤10s; sc failure MONKMODE reset= 0 actions= '' then kill service → guardian restarts it ≤10s (layer-2-only path; restore policy after); expiry → block lifts, service stops AND STAYS stopped (no restart loop), no mm_guard/mm_notify strays, snapshot+teardown clean. Teardown/cleanup.ps1 must also kill mm_guard. Mark ARCHITECTURE B1 'live-verified → Medium' only after it passes. Phase 2 (THREATMODEL.md) stays DEFERRED."
---

# 13/06/2026 — B1 watchdog layer 2: the SYSTEM-child guardian, wired

## Decomposition (ORCHESTRATION §0)
Same shape as layer 1: coupled change on the sacred enforcement surface
(Service1.vb + a new project that must mirror its semantics) → **serialise,
don't fan out**: one writer (orchestrator, full context) + one fresh-context
verifier on the pinned commit (M7/M10). Repo claimed via coordination/ACTIVE.md
(cleared at session end).

## What shipped (commits 395782c, 879e24b)
1. **`MM_guard/MM_guard/` — the guardian** (decision (A) locked 13/06/2026:
   SYSTEM child process, NOT a second service — no new SCM/B6 surface):
   - `Guardian.vb`: pure Friend gates — `BlockHasExpired` (exact copy of the
     service's semantics, parity-tested), `ShouldRestartService` (active block
     + service not running), `ShouldRelaunchNotifier` (mirror of
     `ShouldRestartPeer`, mirror-pinned by test).
   - `Program.vb`: `Global\MonkModeGuardian` single-instance mutex (graceful
     exit if unavailable); 10s loop — read+decrypt `[Time] Until` (any failure
     → "" → fail CLOSED → keep guarding), exit only on parsed+past;
     `ServiceController.Start` for a dead MONKMODE; mm_notify relaunch into
     the interactive session via `WTSGetActiveConsoleSessionId` →
     `WTSQueryUserToken` (primary token, SYSTEM-only) → `CreateEnvironmentBlock`
     → `CreateProcessAsUserW` on `winsta0\default` (verifier confirmed handle
     hygiene on every branch).
   - Per-project `Simple3Des` (returns "" on bad Base64) + `IniFileVb.vb`
     copies, house pattern; `InternalsVisibleTo("MonkMode.Tests")`.
2. **`Service1.vb`**: timer tick wires the already-tested `ShouldRestartPeer`
   → `Process.Start(mm_guard.exe)` while the block is active (Try/Catch, can
   never crash the tick); `stopMe()` best-effort kills mm_guard; cadence
   promoted to `Friend Const TimerIntervalMs = 10000` / `ExpiryGraceSeconds = 5`
   and used at the timer + every timer-path grace site.
3. **Wiring**: MonkMode.sln (+MM_guard), MonkMode.Tests csproj ref,
   tools/build-dist.ps1 (4th exe), README/ARCHITECTURE/CLAUDE/HANDOFF updated.
4. **Tests 95 → 123 green** (`GuardianTests.cs` + extended
   `CryptoRoundTripTests.cs`): gate truth tables incl. negative counts;
   fail-closed ties (unparseable Until keeps restarting service / guardian
   alive); service⇄guardian expiry parity (TheoryData) + peer-gate mirror
   (exhaustive 16-case loop); cadence pins (guardian == service consts AND
   absolute values); 4-copy ciphertext equivalence + CLI→guardian decrypt flow.

## Verifier (fresh context, pinned ref 395782c) — SHIP
- Confirmed: no fail-open path (every corrupt/missing/undecryptable input
  resolves to blockActive=True); no expiry livelock (same grace, same Until on
  both sides — guardian cannot restart a legitimately-stopped service; a tick
  landing mid-stopMe self-exits); P/Invoke marshalling + handle/env/token
  lifecycle leak-free on all branches; WTSQueryUserToken's primary token valid
  for CreateProcessAsUser without duplication; build 0 errors; 123/123.
- P3-1 elevated pre-pin of the guardian → documented residual (ARCHITECTURE B1).
- P3-2 unhandled mutex throw on non-elevated launch → FIXED (graceful exit).
- P3-3 cadence test was a self-mirror → FIXED (pins to Service1's new consts).

## Honest status
B1 = mitigated in **software only**. Severity stays High until the extended
elevated smoke test passes (then Medium). Residuals (ARCHITECTURE B1 row):
double-kill inside the restart windows, recovery-disable + kill (layer 2 now
covers the service side of this), suspend-then-kill, elevated guardian pre-pin,
PPL/kernel-driver-grade immunity out of scope (B10-tier).
